<% if @status == 'success' %>
$('.collection').append('<%= link_to @category.name, tweet_path(id: @tweet.id,category_id: @category.id),
                  method: :patch, remote: true, class: 'collection-item' %>')
<% else %>
console.log('failed');
<% end %>
$('.collapsible').collapsible('close');